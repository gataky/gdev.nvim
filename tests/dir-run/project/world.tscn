[gd_scene load_steps=2 format=3 uid="uid://bgdevrunworld"]

; Lower-case and at the top level on purpose: a directory walk reaches this
; before 'scenes/', so listing scenes has to sort rather than hand back the
; order the filesystem offered.

[ext_resource type="Script" path="res://scripts/player.gd" id="1_player"]

[node name="World" type="Node2D"]
script = ExtResource("1_player")
